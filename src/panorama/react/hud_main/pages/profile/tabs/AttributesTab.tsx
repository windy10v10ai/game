import React from 'react';

/**
 * 属性 Tab 占位。未来承载：玩家可分配的属性点设置（基于会员/勇士等级解锁）。
 */
export function AttributesTab() {
  return (
    <Panel className="placeholder-container">
      <Label className="placeholder-text" text="属性系统开发中..." />
    </Panel>
  );
}
