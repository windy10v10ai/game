import React from 'react';

/**
 * 等级 Tab 占位。未来承载：
 *   - 勇士等级（赛季积分对应等级）
 *   - 会员等级（捐赠/订阅等级）
 */
export function LevelsTab() {
  return (
    <Panel className="placeholder-container">
      <Label className="placeholder-text" text="等级系统开发中..." />
    </Panel>
  );
}
