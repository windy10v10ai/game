---
description: npc_abilities_override.txt 编辑格式；完整流程与注释惯例以 skill 为准
globs:
  - game/scripts/npc/npc_abilities_override.txt
alwaysApply: false
---

# NPC Abilities Override（Cursor 规则）

本规则仅在编辑匹配文件时辅助记忆**不可违背的约束**。**权威流程、加强类型、注释写法**见：

`.claude/skills/update-abilities-override/SKILL.md`

---

## 与 skill 一致的核心约定

1. **差分-only**：与参考**逐字相同**的键不得写入 override（含 `AbilityBehavior`、`SpellImmunityType`、`AbilityCastAnimation`、与参考相同的 `AbilityCastPoint`/`Range` 等）；引擎合并原版即可。
2. **同值不保留**：数值/枚举与参考一致的键**删除**对应行，勿为「完整」抄写，勿仅写备忘注释。`AbilityValues` 内与参考相同的 `special_bonus_unique_*` / `special_bonus_scepter` / `special_bonus_shard` 等**勿抄写**；**不得**写 `CalculateSpellDamageTooltip`（原版合并即可，维护时删旧稿）。
3. **增键**：原则上不增加参考中没有的键；若必须增加，在**该技能块上方**用 `//` 中文说明。
4. **行尾注释**：`//` 后为**当前参考官方数值**作对照；勿写命石、facet 名或已删子键名（详见 skill）。示例：`// 18`、`// 150 130 110`、`// 150 130 110 x2`、`// 200 400 600 800 +100`、`// 10 +20差值`。**与参考同值**的键应**删行**，勿只写备忘注释。
5. **多档字段**：**先**按 skill「同值多档」删整键（优先于补档）。**恒定单值**：本图各等级同一数则只写**单 token**，禁止 `n n n` 凑档。**随等级变化**的多档、且经判定仍须保留者，分档个数须与有效 `MaxLevel` 一致。本图提高 `MaxLevel` 时须按 skill **「MaxLevel 扩展必扫」**：以**参考该技能整段为全集**（块顶多档 + `AbilityValues` 子块 `value` + 顶层平铺），**含「参考有而 override 无」的多档键**做差集，不得只扫 override 已有行；**勿**把参考里本为单档的键硬拉成多档。
6. **行尾 `x2` 等**：为换算规则，换参考后须按规则重算引号内数值，勿只改注释。
7. **Facet**：不得写 `special_bonus_facet_*`（7.41+ 已废）；同档数值与参考完全一致时不要仅为多一档而写该键。

---

## 编辑门禁（格式与边界）

- **缩进**：Tab；与周边块一致。
- **重复**：同一技能块内同一 key 只出现一次。
- **顺序**：若同一技能块内仍有多个键，顺序尽量与参考一致（但不为此增加同值键）。

---

## 参考与检索（`{version}` = 当前 `docs/reference/` 下目录名）

| 用途                                   | 路径                                                       |
| -------------------------------------- | ---------------------------------------------------------- |
| 英雄槽位（Ability1–6，未定技能列表时） | `docs/reference/{version}/npc_heroes.txt`                  |
| 官方技能块                             | `docs/reference/{version}/heroes/npc_dota_hero_<hero>.txt` |
| 中文名 → 系统名                        | `docs/reference/{version}/abilities_schinese.txt`          |

```bash
grep -n "hero_ability_name" game/scripts/npc/npc_abilities_override.txt
```

---

## 快速自检清单

- [ ] 无官方已废弃结构残留。
- [ ] 无 `CalculateSpellDamageTooltip`；无与参考同值的 `special_bonus_unique_*` / `special_bonus_scepter` 等抄写。
- [ ] 无「与参考同值却仍占一行」的键（应删行）；无与参考逐字相同的 `AbilityBehavior` / `AbilityCastAnimation` 等抄写。
- [ ] 无 `special_bonus_facet_*`；无「全相同 token」的多档凑行；无不必要的「每档同值」多档键。
- [ ] 无「无块首说明」的随意增键。
- [ ] **随等级变化**且仍保留在 override 内的多档，条数与 `MaxLevel`（或推断）一致；恒定单值用单 token；同值多档已整键删除的不补档。
- [ ] 已按 skill「MaxLevel 扩展必扫」以**参考全文**为全集：块顶多档 + `AbilityValues` 内**所有**多档（含**无子块**平铺），含 **override 尚未写出**的键，无应延伸却漏写的项。
- [ ] 行尾对照数与**当前 `docs/reference` 版本**中的官方值一致；含 `x2` 等与数值一致。
